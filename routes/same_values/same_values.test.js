describe("Test GET /array", () => {
    test("It should respond with 200 success", () => {
        const response = 200;
        expect (response).toBe(200);
    });
});

describe('Test POST /results', () => {
    test('It should respond with 200 success', () => {

    });

    test('It should catch missing two valid arrays', () => {

    });
});